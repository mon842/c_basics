#include<stdio.h>
// question to check that the syntax is correct or not
int main(){
    int a=2;
    if (a=4)// it also changes the value// it will give true because a contains a value which is not 0 '' if 0 than it's the only case that will show false
    {
        printf("true %d",a);
    }
    else
    {
        printf("false");
    }
    
    
    return 0;
}